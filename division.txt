#include <stdio.h>
int main() {    

    int number6, number2, sum;
    
    printf("Enter two integers: ");
    scanf("%d %d", &number6, &number2);

    // calculating sum
    sum = number6 / number2;      
    
    printf("%d / %d = %d", number6, number2, sum);
    return 0;
}